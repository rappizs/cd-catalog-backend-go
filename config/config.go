package config

const (
	//Host is the DB server host
	Host = "localhost"
	//Port is the DB server's port
	Port = 5432
	//User is the login user for the DB
	User = "postgres"
	//Password is the DB password
	Password = "postgres"
	//DBname is the name of the DB
	DBname = "CDStore"
	//ServerPort ...
	ServerPort = "8000"
)
